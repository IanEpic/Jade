- var category      = content.category
- var criteria      = content.criteria
- var questions     = content.questions
- var eligibilities = content.eligibilities
- var isNew         = content.isNew
- var saved         = content.saved
- var categoryTypes = content.categoryTypes || []
if saved
    p(style='background:#1a4a1a;color:var(--color-text);padding:8px 12px;border-left:4px solid #4caf50;margin-bottom:14px') Category saved.
include ../formCategory-content.pug
